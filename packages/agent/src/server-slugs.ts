/** Default descriptions for well-known servers. Used as fallback when layer.description is empty. */
export const SERVER_SLUGS: Record<string, string> = {
  tricoteuses: 'Legislative data from French Assembly and Senate (schemas, SQL queries, FTS search, TypeScript scripts)',
  autoui: 'Render and manage widgets on the user canvas (charts, tables, maps, timelines, etc.)',
};
