---
title: "@webmcp-auto-ui/core"
description: "MCP client, JSON Schema validation, WebMCP server, type definitions"
sidebar:
  order: 4
---

The `@webmcp-auto-ui/core` package contains W3C WebMCP types, Streamable HTTP MCP client, JSON Schema validation, and helpers.

## W3C WebMCP Types

### `ModelContextTool`

Tool signature following the W3C standard.

```ts
export interface ModelContextTool {
  name: string;                                // Unique, non-empty
  description: string;                        // Human-readable, non-empty
  inputSchema?: JsonSchema;                   // JSON Schema for parameters
  outputSchema?: JsonSchema;                  // JSON Schema for return (optional)
  annotations?: ToolAnnotations;              // Hints (readOnly, destructive, etc.)
}
```

### `ToolAnnotations`

Hints for tool behavior.

```ts
export interface ToolAnnotations {
  readOnlyHint?: boolean;        // Tool does not modify state
  destructiveHint?: boolean;     // Tool is irreversible
  idempotentHint?: boolean;      // Multiple calls = same effect
  openWorldHint?: boolean;       // Interacts with external systems
  cautionHint?: boolean;         // Use with caution
  title?: string;                // Human-readable title (MCP SDK)
}
```

### `JsonSchema`

Union of JSON schemas (compatible with JSON Schema Draft 7+).

```ts
export type JsonSchema = boolean | JsonSchemaObject;

export interface JsonSchemaObject {
  type?: JsonSchemaType | JsonSchemaType[];
  title?: string;
  description?: string;
  properties?: Record<string, JsonSchema>;
  required?: string[];
  items?: JsonSchema | JsonSchema[];
  enum?: unknown[];
  pattern?: string;
  minLength?: number;
  maxLength?: number;
  minimum?: number;
  maximum?: number;
  // ... + composition (allOf, anyOf, oneOf)
}
```

## MCP Client

### `McpClient`

JSON-RPC client for MCP protocol (Streamable HTTP).

```ts
import { McpClient } from '@webmcp-auto-ui/core';

const client = new McpClient(
  'http://localhost:3000/mcp',
  {
    clientName: 'MyApp',
    clientVersion: '1.0',
    timeout: 30000,
  }
);

// Initialize
await client.initialize();

// List tools
const tools = await client.listTools();

// Call a tool
const result = await client.callTool('search_recipes', { query: 'pizza' });
```

### Types

```ts
export interface McpClientOptions {
  clientName?: string;
  clientVersion?: string;
  timeout?: number;                // ms (default: 30000)
  headers?: Record<string, string>;
  autoReconnect?: boolean;
  maxReconnectAttempts?: number;   // Default: 3
}

export interface McpInitializeResult {
  protocolVersion: string;
  capabilities: McpCapabilities;
  serverInfo: McpServerInfo;
}

export interface McpTool {
  name: string;
  description?: string;
  inputSchema?: JsonSchema;
  outputSchema?: JsonSchema;
  annotations?: ToolAnnotations;
}

export interface McpToolResult {
  content: McpToolResultContent[];
  isError?: boolean;
}

export interface McpToolResultContent {
  type: 'text' | 'image' | 'resource';
  text?: string;
  data?: string;        // base64 for images
  mimeType?: string;
  resource?: {
    uri: string;
    mimeType?: string;
    text?: string;
    blob?: string;
  };
}
```

### Example

```ts
const client = new McpClient('http://localhost:3000/mcp');
await client.initialize();

try {
  const result = await client.callTool('describe_image', {
    image_path: '/path/to/image.png',
  });
  
  const textContent = result.content.find((c) => c.type === 'text');
  console.log(textContent?.text);
} catch (err) {
  console.error('Tool failed:', err);
}
```

## Multi-MCP Client

### `McpMultiClient`

Wrapper to manage multiple MCP servers.

```ts
import { McpMultiClient } from '@webmcp-auto-ui/core';

const multi = new McpMultiClient();

// Add servers
await multi.connect('recipe_server', 'http://localhost:3000/mcp');
await multi.connect('image_server', 'http://localhost:3001/mcp');

// List all tools from all servers
const allTools = multi.listAllTools();

// Call a tool (auto-routes)
const result = await multi.callTool('search_recipes', { query: 'pizza' });
```

### Route resolver

Determines which server to call based on tool name.

```ts
// Try search_recipes on each connected server
// Return the first non-error result
```

## JSON Schema Validation

### `validateJsonSchema()`

Validate a value against a JSON schema.

```ts
import { validateJsonSchema } from '@webmcp-auto-ui/core';

const schema: JsonSchema = {
  type: 'object',
  properties: {
    name: { type: 'string' },
    age: { type: 'integer', minimum: 0 },
  },
  required: ['name'],
};

const result = validateJsonSchema(
  { name: 'Alice', age: 30 },
  schema
);

console.log(result.valid);   // true
console.log(result.errors);  // []
```

### Result

```ts
export interface ValidationResult {
  valid: boolean;
  errors: ValidationError[];
}

export interface ValidationError {
  path: string;           // "$.age"
  message: string;        // "Expected integer, got string"
  value?: unknown;
  expected?: unknown;
}
```

## Helpers

### `sanitizeSchema()`

Clean a JSON schema for Anthropic API (removes unsupported fields).

```ts
import { sanitizeSchema } from '@webmcp-auto-ui/core';

const raw = {
  type: 'object',
  $comment: 'Will be removed',
  properties: { name: { type: 'string' } },
};

const clean = sanitizeSchema(raw);
```

### `createToolGroup()`

Create a tool group with namespace.

```ts
import { createToolGroup } from '@webmcp-auto-ui/core';

const group = createToolGroup('database', [
  {
    name: 'query',
    description: 'Execute a SQL query',
    inputSchema: { /* ... */ },
    execute: async (params) => { /* ... */ },
  },
]);

// All tools prefixed: database_query, database_insert, etc.
```

## WebMCP Server

Local server for widgets + UI tools.

### `createWebMcpServer()`

```ts
import { createWebMcpServer } from '@webmcp-auto-ui/core';

const server = createWebMcpServer('myui', {
  description: 'Custom UI widgets',
});

// Register recipes (markdown frontmatter)
server.registerWidget(`
---
widget: my-widget
description: My custom widget
schema:
  type: object
  properties:
    title: { type: string }
---

## Usage
Call widget_display('my-widget', { title: 'Hello' })
`, myWidgetRenderer);

// Add custom tools
server.addTool({
  name: 'custom_action',
  description: 'Custom action',
  inputSchema: { type: 'object', properties: {} },
  execute: async (params) => {
    return { ok: true };
  },
});

// Get the layer for agent loop
const layer = server.layer();
```

### Types

```ts
export interface WebMcpToolDef {
  name: string;
  description: string;
  inputSchema: Record<string, unknown>;
  execute: (params: Record<string, unknown>) => Promise<unknown>;
}

export interface WidgetEntry {
  name: string;
  description: string;
  inputSchema: Record<string, unknown>;
  recipe: string;            // Markdown instructions
  renderer: WidgetRenderer;
  group?: string;
  vanilla: boolean;          // true = function, false = framework component
}

export interface WebMcpServer {
  readonly name: string;
  readonly description: string;
  registerWidget(recipeMarkdown: string, renderer: WidgetRenderer): void;
  addTool(tool: WebMcpToolDef): void;
  layer(): { protocol: 'webmcp'; serverName: string; tools: WebMcpToolDef[] };
  getWidget(name: string): WidgetEntry | undefined;
  listWidgets(): WidgetEntry[];
}

export type WidgetRenderer = 
  | ((container: HTMLElement, data: Record<string, unknown>) => void | (() => void))
  | unknown;  // Svelte component, React component, etc.
```

### Frontmatter Parser

```ts
import { parseFrontmatter } from '@webmcp-auto-ui/core';

const markdown = `
---
widget: stat
description: Key statistic
schema:
  type: object
  properties:
    label: { type: string }
---

## Usage
Call widget_display('stat', { label: 'KPI', value: '42' })
`;

const { frontmatter, body } = parseFrontmatter(markdown);
// frontmatter = { widget: 'stat', description: '...', schema: {...} }
// body = "## Usage\n..."
```

### `mountWidget()`

Mount a vanilla widget in a DOM element.

```ts
import { mountWidget } from '@webmcp-auto-ui/core';
import { autoui } from '@webmcp-auto-ui/agent';

const container = document.getElementById('widget-here');
const cleanup = mountWidget(container, 'stat', { label: 'KPI', value: '42' }, [autoui]);

// Later
cleanup?.();
```

## postMessage Bridge

Communicate with iframes via postMessage (WebMCP cross-window).

### `listenForAgentCalls()`

Listen for tool calls from iframes.

```ts
import { listenForAgentCalls } from '@webmcp-auto-ui/core';

const unregister = listenForAgentCalls(async (name, args) => {
  console.log(`Tool called: ${name}`, args);
  return { result: 'ok' };
});

// Later
unregister();
```

### `callToolViaPostMessage()`

Call a tool in a parent/sibling frame.

```ts
import { callToolViaPostMessage } from '@webmcp-auto-ui/core';

const result = await callToolViaPostMessage(
  window.parent,
  'search_recipes',
  { query: 'pizza' },
  'https://example.com'
);
```

### Types

```ts
export interface WebMCPCallToolEvent {
  type: 'webmcp:call-tool';
  callId: string;
  name: string;
  args?: Record<string, unknown>;
}

export interface WebMCPToolResultEvent {
  type: 'webmcp:tool-result';
  callId: string;
  result: ToolExecuteResult;
}

export interface WebMCPToolErrorEvent {
  type: 'webmcp:tool-error';
  callId: string;
  error: string;
}
```

## Events and async

### `dispatchAndWait()`

Send an event and wait for response.

```ts
import { dispatchAndWait } from '@webmcp-auto-ui/core';

const response = await dispatchAndWait(
  document,
  new CustomEvent('my-event', { detail: { foo: 'bar' } })
);
```

### `signalCompletion()`

Signal that an async operation is complete.

```ts
import { signalCompletion } from '@webmcp-auto-ui/core';

signalCompletion('task-id', { result: 'ok' });
```

## Result helpers

Builders for constructing `ToolExecuteResult`.

### `textResult()`

```ts
import { textResult } from '@webmcp-auto-ui/core';

return textResult('Hello, world!');
// { content: [{ type: 'text', text: '...' }], isError: false }
```

### `jsonResult()`

```ts
import { jsonResult } from '@webmcp-auto-ui/core';

return jsonResult({ message: 'Success', data: [...] });
// { content: [{ type: 'text', text: '{"message":"..."}' }], isError: false }
```

## Navigator API (W3C WebMCP)

Polyfill for `navigator.modelContext` (tool registration in browser).

### `initializeWebMCPPolyfill()`

Install the WebMCP polyfill.

```ts
import { initializeWebMCPPolyfill } from '@webmcp-auto-ui/core';

initializeWebMCPPolyfill({
  forcePolyfill: false,
  allowInsecureContext: false,
  degradeGracefully: true,
  authContext: { userId: 'user-123' },
});
```

### Usage

```ts
navigator.modelContext?.registerTool({
  name: 'my_tool',
  description: 'My custom function',
  inputSchema: { type: 'object', properties: {} },
  execute: async (input) => {
    return {
      content: [{ type: 'text', text: 'Result' }],
    };
  },
  annotations: { readOnlyHint: true },
});
```

## All exports

```ts
// Types
export type {
  JsonSchemaType, JsonSchemaObject, JsonSchema,
  ToolAnnotations, ModelContextTool,
  ToolExecuteCallback, ModelContextClient,
  McpTool, McpToolResult, McpToolResultContent,
  McpCapabilities, McpServerInfo, McpInitializeResult,
  ToolContent, ToolExecuteResult, ToolContentText,
  ToolContentImage, ToolContentResource, ToolContentError,
  // ...
};

// MCP
export { McpClient, McpMultiClient };

// WebMCP
export { createWebMcpServer, parseFrontmatter, mountWidget };

// Validation
export { validateJsonSchema };

// Helpers
export { sanitizeSchema, createToolGroup, textResult, jsonResult };

// Polyfill
export { initializeWebMCPPolyfill, cleanupWebMCPPolyfill };

// postMessage
export { listenForAgentCalls, callToolViaPostMessage };
```
